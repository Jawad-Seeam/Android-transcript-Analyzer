<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--
        Allows FileProvider to share files from the app's internal cache directory.
        Used for temporary camera capture images.
    -->
    <cache-path name="camera_temp" path="." />
</paths>
